import characters from './characters.js';
import sortCharacters from './sort.js';
import healthStatus from './health.js';

console.log(sortCharacters(characters));
console.log(healthStatus(characters[1]));
